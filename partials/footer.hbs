<footer>
  <div class="container">
    <nav id="footer">
      <h1>{{@site.title}}</h1>
      <p>{{@site.description}}</p>
      <ul>
        {{#if @blog.navigation}}
          {{navigation}}
        {{/if}}
      </ul>
    </nav>
  </div>
</footer>
